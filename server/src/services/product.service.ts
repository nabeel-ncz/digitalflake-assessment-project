export const create = async (
    data: { 
        name: string;
        size: number;
        price: number;
        image: string;
        status: string;
        categoryRef: string;
     }
) => {

}

export const update = async (
    data: { 
        _id: string;
        name: string;
        size: number;
        price: number;
        image: string;
        status: string;
        categoryRef: string;
     }
) => {

}

export const findAll = async (
    data: { page: string; limit: string; }
) => {

}

export const findById = async (
    data: { _id: string; }
) => {

}